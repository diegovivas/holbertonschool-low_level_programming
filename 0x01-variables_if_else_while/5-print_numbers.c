

#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	float al;

	for (al = '0'; al <= '9'; al++)

		putchar(al);
	putchar('\n');

	return (0);
}
